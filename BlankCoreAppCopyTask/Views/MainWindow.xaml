<Window x:Class="BlankCoreAppCopyTask.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525" >
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <!-- <ContentControl prism:RegionManager.RegionName="ContentRegion" /> -->
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <Button Content="Clear dest folder" 
                    IsEnabled="{Binding CanCopy}" 
                    Command="{Binding ClearCommand}" 
                    Visibility="{Binding CanCopy, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Margin="15"/>

            <RadioButton 
                GroupName="ready" 
                IsChecked="True"
                Name="Slow"
                Command="{Binding ChangeMethodCommand}"
                CommandParameter="{Binding ElementName=Slow, Path=Content}"
                IsEnabled="{Binding CanCopy}" 
                Visibility="{Binding CanCopy, Converter={StaticResource BooleanToVisibilityConverter}}">
                Slow
            </RadioButton>
            <RadioButton 
                GroupName="ready"
                Name="Fast"
                Command="{Binding ChangeMethodCommand}"
                CommandParameter="{Binding ElementName=Fast, Path=Content}"
                IsEnabled="{Binding CanCopy}" 
                Visibility="{Binding CanCopy, Converter={StaticResource BooleanToVisibilityConverter}}">
                Fast
            </RadioButton>

            <Button Content="Copy" 
                    Command="{Binding CopyCommand}" 
                    IsEnabled="{Binding CanCopy}" 
                    Visibility="{Binding CanCopy, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Margin="15"/>
            <ProgressBar 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Height="10" 
                Width="500"
                Value="{Binding ProgressValue, Mode=TwoWay}"
                IsIndeterminate="False"
                Minimum="0"
                Maximum="1"/>

            <TextBlock Text="{Binding LogMessage}"/>
        </StackPanel>
    </Grid>
</Window>
